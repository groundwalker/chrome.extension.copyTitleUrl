# chrome.extension.copyTitleUrl

## とりあえず使ってみる

git clone

chrome://extensions へ行く

`Load unpacked extension` からcloneしたディレクトリを選択してインストール

右下の `Keyboard shortcuts` から `CopyTitleAndUrl` にショートカットを割り当てる

## カスタマイズ

`background.js` の text_area.value を設定しているところを好きに変更するとよいです。
